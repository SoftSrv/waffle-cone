{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": ""
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "us-east-1",
    "source_ami": "ami-a4c7edb2",
    "instance_type": "t2.micro",
    "ssh_username": "ec2-user",
    "ssh_keypair_name": "common-key",
    "ssh_agent_auth": true,
    "ami_name": "packer-example {{timestamp}}"
  }],
  "provisioners": [
    {
      "type": "shell",
      "script": "prov.sh"
    }
  ]
}
